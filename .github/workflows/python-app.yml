name: Build and Release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: '版本號 (例如 v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write # 必須要有寫入權限才能建立 Release

jobs:
  build:
    strategy:
      matrix:
        # 一次跑兩台機器：Windows 和 M1 Mac
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install nuitka
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        shell: bash # 強制用 bash 處理兩者差異

      - name: Build with Nuitka
        run: |
          python3 -m nuitka --standalone --onefile --windows-console-mode=disable --macos-create-app-bundle --enable-plugin=pyside6 --windows-icon-from-ico=icon.ico --macos-app-icon=icon.png --include-data-files=icon.ico=icon.ico --include-data-files=icon.png=icon.png --assume-yes-for-downloads main.py

      - name: Create Release and Upload
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag_name }}
          name: Release ${{ github.event.inputs.tag_name }}
          # 指定要上傳的副檔名 (Windows 是 .exe，Mac 沒副檔名或 .app)
          files: |
            main.exe
            main.app
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
